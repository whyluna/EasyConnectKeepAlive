# Changelog

## 1.0.2 - 2026-08-26

- 改用带“Applications”快捷入口的 DMG 发布，不再使用 ZIP。
- README 使用更宽、更清晰的启用态应用截图。
- 默认窗口尺寸调整为 900×820，提升配置与日志可读性。
- CI 改为构建和上传 DMG。

## 1.0.1 - 2026-08-26

- 首个公开版本。
- 支持 HTTPS、HTTP 和 TCP 保活请求。
- 支持自定义地址、端口、请求方式、路径、TCP 内容和发送间隔。
- 可要求 EasyConnect 隧道进程在线，并验证目标路由经过 `utun`。
- 提供单次测试、开始、停止和运行日志。
- HTTP GET 默认使用 Range 请求，在服务器支持时仅下载 1 字节。
- 禁用请求代理，避免保活流量误走本地 HTTP/SOCKS 代理。
- 提供原生 macOS 图标和 arm64/x86_64 通用应用包。
